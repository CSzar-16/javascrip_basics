const name = "chirag "
let repoCount = 27

// older way probably not recommended
console.log(name + repoCount);

// string interpolation modern way 
console.log(`Hello my name is ${name} and my age is ${repoCount}`)